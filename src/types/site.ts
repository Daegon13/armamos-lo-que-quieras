export type NavItem = {
  label: string;
  href: string;
};

export type BusinessInfo = {
  brand: string;
  tagline: string;
  phoneDisplay: string;
  whatsappNumber: string;
  emailDisplay: string;
  serviceArea: string;
};
