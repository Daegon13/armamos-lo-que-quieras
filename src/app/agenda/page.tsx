import { PagePlaceholder } from "@/components/sections/PagePlaceholder";

export default function AgendaPage() {
  return (
    <PagePlaceholder
      title="Agenda"
      description="Esta sección quedará lista para centralizar la reserva de horarios y coordinación de visitas a domicilio."
    />
  );
}
