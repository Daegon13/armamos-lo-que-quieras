import { PagePlaceholder } from "@/components/sections/PagePlaceholder";

export default function AgendaPage() {
  return (
    <PagePlaceholder
      title="Agenda"
      description="Estamos preparando el flujo de reserva de horarios para el siguiente parche."
    />
  );
}
